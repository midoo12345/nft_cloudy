{
  "name": "web3-certificate-system",
  "version": "1.0.0",
  "description": "A Web3 certificate system using NFTs and IPFS",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network",
    "start": "cd client && npm start"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.5.0",
    "hardhat": "^2.19.5",
    "ipfs-http-client": "^60.0.1",
    "pinata": "^2.2.0",
    "web3": "^4.5.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "chai": "^4.3.10",
    "ethers": "^6.11.1"
  }
}
